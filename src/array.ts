var arr : number[] = []; //declearing an array 

// this will restrect the array to number type

arr[0] = 45;
// arr[1] = "safi"  // this will throw CTE because you are trying to assign string value in array of number type

